export const ENV = {
  ownerId: process.env.OWNER_ID || "clp61s600000008l412r5117u", // Replace with actual owner ID
  cookieSecret: process.env.COOKIE_SECRET || "super-secret-cookie-key", // Replace with a strong secret key
};

